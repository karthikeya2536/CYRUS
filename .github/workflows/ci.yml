name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: npm build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run build

  deno-check:
    name: deno check edge functions
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: denoland/setup-deno@v1
        with:
          deno-version: v2.x
      - name: Type-check every edge function
        run: |
          set -e
          fail=0
          for d in supabase/functions/*/; do
            f="$d/index.ts"
            [ -f "$f" ] || continue
            echo "::group::deno check $f"
            if ! deno check "$f"; then fail=1; fi
            echo "::endgroup::"
          done
          exit $fail

  deno-test:
    name: deno test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: denoland/setup-deno@v1
        with:
          deno-version: v2.x
      - name: Run Deno tests (temporal, retrieval)
        run: |
          set -e
          for f in supabase/functions/*/*.test.ts; do
            [ -f "$f" ] || continue
            echo "::group::deno test $f"
            deno test --allow-read --allow-net "$f"
            echo "::endgroup::"
          done

  migrations:
    name: migration validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: supabase/setup-cli@v1
        with:
          version: latest
      - name: Boot local stack (applies migrations)
        run: supabase start
      - name: Fresh reset (strict ordered re-apply of all migrations)
        run: supabase db reset
      - name: Run retrieval validation scripts
        run: |
          for script in scripts/validate-dedup.sql scripts/validate-graph.sql; do
            [ -f "$script" ] || continue
            echo "::group::$script"
            PGPASSWORD=postgres psql -h localhost -p 54322 -U postgres -d postgres -f "$script"
            echo "::endgroup::"
          done
      - name: Stop stack
        if: always()
        run: supabase stop || true
